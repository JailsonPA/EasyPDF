<UserControl x:Class="EasyPDF.UI.Views.PdfViewerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:EasyPDF.Application.ViewModels;assembly=EasyPDF.Application"
             xmlns:ctrl="clr-namespace:EasyPDF.UI.Controls"
             xmlns:conv="clr-namespace:EasyPDF.UI.Converters"
             Background="{DynamicResource PageBackgroundBrush}">

    <UserControl.Resources>
        <conv:BoolToVisibilityConverter x:Key="BoolToVis" />
    </UserControl.Resources>

    <!-- The ScrollViewer is the outer panning surface.
         MouseWheel zoom is handled in code-behind to prevent scroll interference. -->
    <!-- CanContentScroll="True" must live on the ScrollViewer so the VirtualizingStackPanel
         can register it as its scroll owner and pixel-virtualise correctly.
         Placing it on the ItemsControl (as an attached property) has no effect
         because ItemsControl is not the scroll host — the ScrollViewer is. -->
    <ScrollViewer x:Name="MainScroller"
                  HorizontalScrollBarVisibility="Auto"
                  VerticalScrollBarVisibility="Auto"
                  CanContentScroll="True"
                  PanningMode="Both"
                  Background="Transparent">

        <!-- Virtualized page list. Each page's Width/Height is scaled via PageViewModel.DisplayWidth/Height,
             so the ItemsControl always has the correct scrollable extent without a LayoutTransform. -->
        <ItemsControl x:Name="PageList"
                      ItemsSource="{Binding Pages}"
                      HorizontalAlignment="Center"
                      VirtualizingPanel.IsVirtualizing="True"
                      VirtualizingPanel.VirtualizationMode="Recycling"
                      VirtualizingPanel.ScrollUnit="Pixel">

            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <VirtualizingStackPanel Orientation="Vertical" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>

            <ItemsControl.ItemTemplate>
                <DataTemplate DataType="{x:Type vm:PageViewModel}">
                    <ctrl:PdfPageControl
                        DataContext="{Binding}"
                        ViewerVm="{Binding DataContext, RelativeSource={RelativeSource AncestorType=UserControl}}"
                        Width="{Binding ControlWidth}"
                        Height="{Binding ControlHeight}"
                        Margin="0,4" />
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </ScrollViewer>
</UserControl>
